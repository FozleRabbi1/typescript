"use strict";
{
    const numberOfArray = [1, 2, 3, 4, 5];
    const stringOfArray = numberOfArray.map(n => n.toString());
    const area2 = {
        height: '100',
        width: 50
    };
    // console.log(area2);
}
